# Digiworld Agent Guidelines

## UI 风格

Digiworld 的界面是面向 Windows 桌面端的简洁、紧凑型数据工具风格，支持完整浅色和深色主题。新增或修改 UI 时，应遵循 `docs/ui-design.md` 和以下规范：

- 主程序使用 `@digiworld/typography` 提供的字体和字号 token，并通过 `@digiworld/design-system/tokens.css` 共享颜色、圆角、阴影和动效。界面支持 Plex 灵动、霞鹜文楷和 Windows 原生三套全局字体预设；宿主负责向隔离插件页提供字体声明和 `--dw-font-sans`、`--dw-font-display`、`--dw-font-brand`，插件不得依赖或打包字体文件，也不要在局部硬编码字体栈。
- 使用 Catppuccin Latte／Mocha 和 Rosé Pine Dawn／Moon 四套语义主题。背景、卡片、文字、状态和图表都服从宿主，禁止写死浅色背景或白色按钮文字。
- 通用控件使用 `@digiworld/design-system/react`（其他技术栈可使用同包 CSS 类），统一字号、行高、圆角、焦点与状态；插件 CSS 只处理业务布局和可视化。正文默认 14px，支持宿主 100%／110%／125% 文字缩放。
- 新插件版本声明 `uiDesignVersion: 1`，打包前执行 `pnpm check:ui`、构建及完整 `pnpm test:ui`，禁止跳过或伪造与最终 UI 哈希关联的验证记录。
- 优先复用宿主提供的 `--dw-*` 主题变量和共享设计 token，不在插件中另建冲突的配色、圆角、阴影或动效体系。
- 布局强调信息密度和清晰层级：左侧导航、顶部工具栏、内容区卡片和数据面板结构明确，间距紧凑但不拥挤。
- 控件、卡片、浮层分别使用 8／12／16px 圆角；使用轻阴影，卡片悬停不浮起，不使用持续背景动效。玻璃效果默认关闭，已有用户选择保留。
- 图标保持细线条和小尺寸，按钮以明确的主按钮、次按钮及危险按钮区分优先级；悬停、激活、禁用、加载、成功和失败状态应有一致且可辨认的反馈。
- 信息层级通过字号、字重、留白和颜色建立。标题简洁，正文与辅助文字偏小，关键数值适度突出，不依赖大面积渐变或夸张动效。
- 动效应短促、低调，仅用于状态切换、侧栏收合和加载反馈。所有可交互元素需保留清晰的键盘焦点样式。
- 插件 UI 应与宿主视觉一致，并在较窄或较矮窗口中合理降列、滚动或重排，避免内容溢出和文字遮挡。

## Git 工作流

每完成一项功能修改并完成必要验证后，都必须执行以下流程：

1. 检查工作区，只暂存本次功能相关的文件，不混入用户或其他任务的未提交改动。
2. 使用清晰、准确的提交信息执行 `git commit`。
3. 将当前分支推送到其上游远程，执行 `git push`；若尚未设置上游，则使用 `git push -u origin <当前分支>`。
4. 如果提交或推送失败，应保留现有改动并明确报告错误，不得声称已经完成提交或推送。

`main` 的每次新提交会以当前源码版本和远端稳定 SemVer 标签中的较大者为基准，将补丁号 `z + 1`，直接生成 `vX.Y.Z Preview`。不要恢复 `-alpha.N` 标签，也不要让插件包重复携带宿主字体。
