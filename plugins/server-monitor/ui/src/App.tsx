import{useEffect,useState}from'react';import{Button,Input,Card,Status}from'@digiworld/design-system/react';import{createPluginBridge}from'@digiworld/plugin-sdk';import{Server,Plus,RefreshCw,HardDrive,MemoryStick,Cpu,Gauge,Network,Settings,X}from'lucide-react';
const bridge=createPluginBridge('io.github.jesmonx.digiworld.server-monitor');type Config={id:string;label:string;host:string;disks:string[];interfaces:string[];showCpu:boolean;showGpu:boolean;showTraffic:boolean};type Disk={device:string;mount:string;total:number;used:number;percent:number};type Net={name:string;receivedBytes:number;sentBytes:number;receivedPerSecond:number|undefined;sentPerSecond:number|undefined};type Device={id:string;label:string;hostname:string;timestamp:number;uptimeSeconds:number;memory:{total:number;used:number};cpu:{logicalCores:number;load1:number;load5:number};disks:Disk[];gpus:{index:number;name:string;utilization:number;memoryUsedMiB:number;memoryTotalMiB:number;temperatureC:number}[];network:Net[];vnstat?:{interfaces?:{name:string;traffic?:{day?:{date:{year:number;month:number;day:number};rx:number;tx:number}[]}}[]};selection:Config};const previous=new Map<string,{at:number;rx:number;tx:number}>();const size=(v:number)=>`${(v/1024**3).toFixed(1)} GB`;const rate=(v:number)=>v<1024?`${v.toFixed(0)} B/s`:v<1024**2?`${(v/1024).toFixed(1)} KB/s`:`${(v/1024**2).toFixed(1)} MB/s`;const pct=(a:number,b:number)=>b?Math.round(a/b*100):0;
export default function App(){const[configs,setConfigs]=useState<Config[]>([]);const[devices,setDevices]=useState<Device[]>([]);const[draft,setDraft]=useState<Config|null>(null);const[error,setError]=useState('');const[busy,setBusy]=useState(false);const[setup,setSetup]=useState('');const load=async()=>{setBusy(true);try{const s=await bridge.request<{devices:Config[]}>('servers.settings.get');setConfigs(s.devices);if(s.devices.length)setDevices((await bridge.request<{devices:Device[]}>('servers.sample')).devices.map(d=>({...d,network:d.network.map(n=>{const key=`${d.id}:${n.name}`,p=previous.get(key),seconds=p?Math.max(1,d.timestamp-p.at):1;previous.set(key,{at:d.timestamp,rx:n.receivedBytes,tx:n.sentBytes});return{...n,receivedPerSecond:p&&n.receivedBytes>=p.rx?(n.receivedBytes-p.rx)/seconds:undefined,sentPerSecond:p&&n.sentBytes>=p.tx?(n.sentBytes-p.tx)/seconds:undefined}})})))}catch(e){setError(String(e))}finally{setBusy(false)}};useEffect(()=>{void load();bridge.ready();const t=setInterval(()=>void load(),5000);return()=>clearInterval(t)},[]);const edit=(c?:Config)=>setDraft(c?{...c}:{id:crypto.randomUUID().replaceAll('-','').slice(0,12),label:'',host:'',disks:[],interfaces:[],showCpu:true,showGpu:true,showTraffic:true});const save=async()=>{if(!draft)return;const next=[...configs.filter(x=>x.id!==draft.id),draft];await bridge.request('servers.settings.save',{settings:{devices:next}});setDraft(null);await load()};const vnstat=async(install:boolean)=>{if(!draft)return;setBusy(true);try{const r=await bridge.request<{status:string;command:string;error?:string;verification?:string}>('servers.vnstat.setup',{host:draft.host,install});setSetup([r.status,r.error,r.verification,r.command].filter(Boolean).join('\n'))}catch(e){setError(String(e))}finally{setBusy(false)}};return <main><header className="dw-toolbar"><div><Server size={18}/><strong>远程 Linux 设备</strong></div><Button onClick={()=>edit()}><Plus size={15}/>添加设备</Button><Button onClick={()=>void load()} disabled={busy}><RefreshCw size={15}/>刷新</Button></header>{error&&<Status tone="error">{error}</Status>}<section className="devices">{configs.length===0?<Status>使用 OpenSSH 配置别名添加第一台 Linux 设备。</Status>:devices.map(d=><Card key={d.id}><header><div><Server/><span><strong>{d.label}</strong><small>{d.hostname} · 运行 {Math.floor(d.uptimeSeconds/86400)} 天</small></span></div><Button onClick={()=>edit(configs.find(x=>x.id===d.id))}><Settings size={15}/></Button></header><div className="metrics"><Metric icon={<MemoryStick/>} title="内存" value={`${size(d.memory.used)} / ${size(d.memory.total)}`} percent={pct(d.memory.used,d.memory.total)}/>{d.selection.showCpu&&<Metric icon={<Cpu/>} title="CPU Load" value={`${d.cpu.load1.toFixed(2)} / ${d.cpu.logicalCores} 核`} percent={Math.min(100,d.cpu.load1/d.cpu.logicalCores*100)}/>}</div><div className="disk-grid">{d.disks.filter(x=>!d.selection.disks.length||d.selection.disks.includes(x.mount)).map(x=><Metric key={x.mount} icon={<HardDrive/>} title={`${x.mount} · ${x.device}`} value={`${size(x.used)} / ${size(x.total)}`} percent={x.percent}/>)}</div>{d.selection.showGpu&&<div className="gpu-grid">{d.gpus.length?d.gpus.map(g=><Metric key={g.index} icon={<Gauge/>} title={`GPU ${g.index} · ${g.name}`} value={`${g.utilization}% · ${g.memoryUsedMiB}/${g.memoryTotalMiB} MiB · ${g.temperatureC}°C`} percent={g.utilization}/>):<small>NVIDIA GPU 不可用</small>}</div>}{d.selection.showTraffic&&<div className="traffic"><Network size={16}/>{d.network.filter(x=>!d.selection.interfaces.length||d.selection.interfaces.includes(x.name)).map(n=><span key={n.name}><strong>{n.name}</strong> ↓ {n.receivedPerSecond===undefined?'采样中':rate(n.receivedPerSecond)} ↑ {n.sentPerSecond===undefined?'采样中':rate(n.sentPerSecond)} · 累计 ↓ {size(n.receivedBytes)} ↑ {size(n.sentBytes)}</span>)}<small>{d.vnstat?'vnStat 每日记录已连接':'未检测到 vnStat，当前仅显示网卡累计计数'}</small>{d.vnstat?.interfaces?.filter(v=>!d.selection.interfaces.length||d.selection.interfaces.includes(v.name)).map(v=><div className="daily" key={v.name}><strong>{v.name} · 每日流量</strong>{(v.traffic?.day||[]).slice(-30).reverse().map(x=><span key={`${x.date.year}-${x.date.month}-${x.date.day}`}><time>{x.date.month}/{x.date.day}</time><em>↓ {size(x.rx)} · ↑ {size(x.tx)}</em></span>)}</div>)}</div>}</Card>)}</section>{draft&&<Card className="editor"><header><h2>设备设置</h2><Button onClick={()=>setDraft(null)}><X size={16}/></Button></header><label>名称<Input value={draft.label} onChange={e=>setDraft({...draft,label:e.target.value})}/></label><label>OpenSSH Host 别名<Input value={draft.host} onChange={e=>setDraft({...draft,host:e.target.value})}/></label><div className="toggles">{(['showCpu','showGpu','showTraffic'] as const).map((k,i)=><label key={k}><input type="checkbox" checked={draft[k]} onChange={e=>setDraft({...draft,[k]:e.target.checked})}/>{['CPU','NVIDIA GPU','Traffic'][i]}</label>)}</div><p>选择要并列显示的挂载点和网卡；空选择表示全部显示。</p>{devices.find(x=>x.id===draft.id)&&<div className="selectors"><fieldset><legend>硬盘 / 挂载点</legend>{devices.find(x=>x.id===draft.id)!.disks.map(x=><label key={x.mount}><input type="checkbox" checked={draft.disks.includes(x.mount)} onChange={e=>setDraft({...draft,disks:e.target.checked?[...draft.disks,x.mount]:draft.disks.filter(v=>v!==x.mount)})}/>{x.mount} · {x.device}</label>)}</fieldset><fieldset><legend>网卡</legend>{devices.find(x=>x.id===draft.id)!.network.map(x=><label key={x.name}><input type="checkbox" checked={draft.interfaces.includes(x.name)} onChange={e=>setDraft({...draft,interfaces:e.target.checked?[...draft.interfaces,x.name]:draft.interfaces.filter(v=>v!==x.name)})}/>{x.name}</label>)}</fieldset></div>}<div className="vnstat"><Button onClick={()=>void vnstat(false)}>检测 vnStat</Button><Button onClick={()=>void vnstat(true)} variant="primary">安装并启用</Button></div>{setup&&<pre>{setup}</pre>}<footer><Button onClick={()=>setDraft(null)}>取消</Button><Button variant="primary" onClick={()=>void save()} disabled={!draft.label||!draft.host}>保存</Button></footer></Card>}</main>}
function Metric({icon,title,value,percent}:{icon:React.ReactNode;title:string;value:string;percent:number}){return <div className="metric"><div>{icon}<span><small>{title}</small><strong>{value}</strong></span></div><progress value={percent} max="100"/></div>}
