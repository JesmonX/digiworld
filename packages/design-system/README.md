# Digiworld design tokens

Shared semantic colors, radii, shadows, and motion values for the desktop shell
and official plugins. Plugins should consume the `--dw-*` variables and retain
fallback values so older hosts remain compatible.
