# 十项优化代码复审补修交接

2026-09-08，基于 `d092ef0`。本次按用户要求仅修改代码、补充测试，**没有运行测试、构建、类型检查、格式检查或打包**。此前验收记录不代表本次补修已验证。

## 修改

- 邮件头和正文使用同一份服务器 ALL UID 快照；下一轮补抓新到 UID，缺失邮件头也不受旧游标限制。正文只处理已索引 UID，取消空邮件头占位写入。保留已有缓存。
- 日历删除只检查父集合的 unbind 能力，不以事件资源缺少 unbind 拒绝删除；仍由 DELETE 响应裁决权限，保留 If-Match。
- XML 处理 GeneralRef，解码预定义和数字引用；保留实体两侧文本空格，未知实体显式报错。
- 隐藏 AGY 页使用自身配置状态；所有 reset 页参与同一 Grid 槽位尺寸计算，非当前页不可见、不可聚焦。
- 安装及更新进度按 itemId 和 manifest 翻译显示名称；没有 itemId 的总体状态使用本地化 fallback，核心更新保持版本名称。
- Agent 插件版本提升为 0.1.24，Mail / Calendar 提升为 0.1.10；未生成新包或 catalog。

## 已补充但未运行的测试

- Mail engine：两轮 UID 快照中新到邮件和失败邮件头的重试选择。
- Calendar backend：父日历允许删除时首个请求直接为 DELETE，并携带 If-Match；XML 名称及 href 的实体、数字引用、空格、未知实体。
- Agent UI：仅配置 AGY 时隐藏页内容稳定；reset 轮播保留全部尺寸占位、仅一个活动页。
- Browser refinement：超长 reset 标题在切换前后保持卡片高度，隐藏页 inert。
- Desktop pluginNames：安装、更新、失败总体状态、第三方翻译、核心更新和无进度状态。

## 下一位执行者

1. 运行相关 Rust 测试、前端单元测试、类型检查和格式检查，处理失败；执行完整项目构建与 UI 验证。
2. QQ 和 iCloud 的真实环境验收仍需另行完成。此次未自动修复此前可能已生成的空邮件头记录，避免误判合法无主题邮件；如用户已有此类记录，应先针对明确 UID 诊断再修复。
3. 使用最终验证产物重新打包三个受影响插件并重建 catalog；旧 UI 哈希报告不能用于此次发布。
4. 验证后按 AGENT.MD 提交、推送并跟踪发布。本次留下未提交改动，未触发远程 CI。
